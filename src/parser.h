#ifndef PARSER_H
#define PARSER_H
#include "main.h" //include main so each respective cpp file only needs to include itself
// #include "parser.h"

// initializes the global trainArray with the data provided by the respective source files
//calls printTrainMatrix() once the trainArray is initialized
void synthTrainMatrix();

#endif